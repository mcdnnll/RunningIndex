import React, {PropTypes} from 'react';
import reactabular from 'reactabular';

const propTypes = {};

const defaultProps = {};

class RunTable extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div></div>
    );
  }
}

RunTable.propTypes = propTypes;
RunTable.defaultProps = defaultProps;

export default RunTable;
