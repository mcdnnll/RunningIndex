const expect = require('chai').expect;
const rewire = require('rewire');

describe('utils/processRunData', () => {

  describe('bestRunSummary', () => {
    it('should return an object with 6 keys', () => {

    });

    it('should return an object with integer values >= 0', () => {

    });

  });
});
